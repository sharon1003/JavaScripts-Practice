function NQueens(number) 
{
    let arr = [];
    for(let i = 0; i < number; i++)
    {
        arr[i] = [];
    }
    for(let i = 0; i < number; i++)
    {
        for(let j = 0; j < number; j++)
        {
            arr[i][j] = null;
        }
    }

    let i = 0;
    let j = 0;
    let loop = true;
    while(loop)
    {
        arr[i][j] = "Q"
        console.log("Okay");

        let violation = false;
        let k = 0;
        // 左上
        if()
    }

};

NQueens(4);